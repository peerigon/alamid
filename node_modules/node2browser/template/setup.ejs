window.Setup = function(modules) {
    var requiringModules = {},
        initializedModules = {};

    <%= getLookUpPaths %>
    <%= initModule %>
    <%= normalizePath %>
    <%= Require %>
    <%= Resolve %>

    initModule('<%= firstModule %>');
};

window.modules = {};
window.global = {};

<%= modules %>
window.Setup(window.modules);
window.Setup = undefined;
