var message = require('folder1');

function run() {
    nodeunit.run({
        browserTest: {
            "Is everything ok?": function(test) {
                test.equal(message, 'Everything is ok');
                test.done();
            }
        }
    });
}

module.exports = run;
