"use strict";

//TODO make routes dynamic via config if set
var routes = {
    services : '/services',
    validators : '/validators',
    pages : '/pages', //not really dynamic, just for recompile
    staticBundle : '/'
};

module.exports = routes;