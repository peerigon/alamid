"use strict";

var schemaRegistry = require("alamid/lib/shared/registries/schemaRegistry.js");

<% _(schemas).each(function eachSchema(schemaPath, schemaUrl)  {
     schemaRegistry.setSchema("<%= schemaUrl %>", require("<%= schemaPath %>");
<% }); %>