"use strict";

var serviceRegistry = require("<%= rootPath %>/node_modules/alamid/lib/shared/registries/serviceRegistry.js");
var ServiceClasses = [];

<% _(services).each(function eachService(servicePath, serviceUrl)  { %>
ServiceClasses["<%= serviceUrl %>"] = require("<%= servicePath %>");
serviceRegistry.setService("<%= serviceUrl %>", new ServiceClasses["<%= serviceUrl %>"]());
<% }); %>