"use strict";

var modelRegistry = require("../../shared/registries/modelRegistry.js");

function populateModelRegistry() {
    var models = {};

<% _(models).each(function eachModel(modelPath, modelUrl)  { %>
    models[<%= JSON.stringify(modelUrl) %>] = require(<%= JSON.stringify(modelPath) %>);
<% }); %>

    modelRegistry.setModels(models);

    return models;
}

module.exports = populateModelRegistry;