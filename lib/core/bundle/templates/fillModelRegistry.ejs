"use strict";

var modelRegistry = require(<%= JSON.stringify(rootPath) %> + "/node_modules/alamid/lib/shared/registries/modelRegistry.js");

<% _(models).each(function eachModel(modelPath, modelUrl)  { %>
     modelRegistry.setModel(<%= JSON.stringify(modelUrl) %>, require(<%= JSON.stringify(modelPath) %>));
<% }); %>