"use strict";

var modelRegistry = require("alamid/lib/shared/registries/modelRegistry.js");

<% _(models).each(function eachModel(modelPath, modelUrl)  { %>
     modelRegistry.setModel("<%= modelUrl %>", require("<%= modelPath %>"));
<% }); %>